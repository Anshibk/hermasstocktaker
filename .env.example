# Local development defaults
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/hermas_stock
# Generate at least 32 random bytes when running in production
SESSION_SECRET=aa84c643329f6cc9d7b7c3f0f3c5e73d8b5d4167eb74dcd3f3d3a2e6f7a1c4d1
SESSION_COOKIE_SECURE=false
GOOGLE_ALLOWED_DOMAIN=gmail.com
GOOGLE_SUPERUSER_EMAIL=your-superuser@gmail.com
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=https://your-domain.com/api/v1/auth/google/callback

# In production (Railway, Render, etc.) set the following **in the hosting dashboard**:
#   DATABASE_URL   -> use the managed Postgres URL the provider gives you
#   SESSION_SECRET  -> generate with `python -c "import secrets; print(secrets.token_hex(32))"`
#   SESSION_COOKIE_SECURE -> leave true in production to force HTTPS cookies
#   GOOGLE_ALLOWED_DOMAIN -> set to the Gmail/Google Workspace domain you accept
#   GOOGLE_SUPERUSER_EMAIL -> the Gmail that bootstraps the tenancy
#   GOOGLE_CLIENT_ID / GOOGLE_CLIENT_SECRET / GOOGLE_REDIRECT_URI -> OAuth credentials from Google
# Do not commit real production secrets to git.

